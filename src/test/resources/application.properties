# Tests use Testcontainers; do not start compose.yaml from the main module.
spring.docker.compose.enabled=false
# Do not run @Scheduled jobs during tests; tests invoke IdempotencyCleanupJob manually.
spring.task.scheduling.enabled=false
